#pragma warning(disable:4996)
#include<stdio.h>
#define SIZE 16

typedef long long lld;

char cardnum(lld num)
{
	int validate = 0;
	int tmp;

	for (int i = 0; i < SIZE; i++)
    {	
		tmp = (num % 10);
		
		if ((i + 1) % 2 == 0)
        {
			if (tmp * 2 >= 10)
            {
				tmp = 2 * tmp - 9;
				validate += tmp;
			}
			else
            {
				validate += (tmp * 2);
			}
		}
		else
        {
			validate += tmp;
		}
		num /= 10;
	}
	if (validate % 10 == 0)
    {
		return 'T';
	}
	else
    {
		return 'F';
	}
}

int main(void)
{
	int n;
	scanf("%d", &n);

	for (int i = 0; i < n; i++)
    {
		lld k;
		scanf("%lld", &k);
		printf("%c\n", cardnum(k));
	}
	return 0;
}
